[package]
name = "rll"
version = "1.0.0"
edition = "2021"

[dependencies]
ratatui = { version = "0.29", default-features = false, features = ["crossterm"] }

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"
